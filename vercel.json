{
    "builds": [{
        "src": "propscan_backend/asgi.py",

        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "propscan_backend/asgi.py"
        }
    ]
}